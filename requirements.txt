numpy>=1.21
scipy>=1.7
matplotlib>=3.4
tqdm>=4.62

# Optional: for ANN conversion
# torch>=1.9

# Optional: for MNIST example
# scikit-learn>=0.24

# Optional: hardware backends
# lava-nc            # Intel Loihi 2 via Lava (pip install lava-nc)
# pynn_brainscales   # BrainScaleS-2 (apply at ebrains.eu)
# hxtorch            # BrainScaleS-2 PyTorch interface
