numpy>=1.21
scipy>=1.7
matplotlib>=3.4
tqdm>=4.62

# Optional: for ANN conversion
# torch>=1.9

# Optional: for MNIST example
# scikit-learn>=0.24

# Optional: hardware backends (requires special access)
# nxsdk              # Intel Loihi 2 (apply at intel.com/loihi)
# pynn_brainscales   # BrainScaleS-2 (apply at ebrains.eu)
# hxtorch            # BrainScaleS-2 PyTorch interface
